export enum EventKeys {
    LOADER = 'setLoader',
    LOGIN = 'login',
    LOGOUT = 'logout',
    GO_TO = 'go-to',
    PUSH_STATE = 'pushstate',
    POP_STATE = 'popstate',
    SUSPENSER = 'suspenser',
    SNACKBAR = 'snackbar',
    SNACKBAR_DEACTIVATE = 'snackbar-deactivate',
}